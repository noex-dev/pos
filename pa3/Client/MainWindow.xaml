<Window x:Class="Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Client"
        mc:Ignorable="d"
        Title="3. praktische Arbeit" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Grid.Column="0">Namenssuche</Label>
        <TextBox x:Name="queryName" Grid.Row="0" Grid.Column="1"/>

        <Label Grid.Row="1" Grid.Column="0">Geschlecht</Label>
        <ComboBox x:Name="querySex" Grid.Row="1" Grid.Column="1">
            <ComboBoxItem>Mann</ComboBoxItem>
            <ComboBoxItem>Frau</ComboBoxItem>
        </ComboBox>

        <Button x:Name="sendQuery" Grid.Row="2" Grid.ColumnSpan="2" Click="SendQuery_Click">Suchen</Button>

        <Label Grid.Row="3" Grid.Column="0">Gefundene Namen</Label>
        <ComboBox x:Name="foundNames" Grid.Row="3" Grid.Column="1" SelectionChanged="foundNames_SelectionChanged"/>

        <Label Grid.Row="4" Grid.Column="0">Alternativen</Label>
        <ComboBox x:Name="foundAlternatives" Grid.Row="4" Grid.Column="1" SelectionChanged="foundAlternatives_SelectionChanged"/>

        <Separator Grid.Row="5" Grid.ColumnSpan="2"></Separator>

        <ListBox x:Name="detailList" Grid.Row="6" Grid.ColumnSpan="2">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Year}" Width="300"/>
                        <TextBlock Text="{Binding Count}" Width="Auto"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        
    </Grid>
</Window>
